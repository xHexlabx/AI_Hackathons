"""{{TITLE}} - agent skeleton for kaggle-environments.

kaggle-environments picks the LAST callable defined in this file, so keep `agent` last.
"""

from __future__ import annotations


def agent(obs, config=None):
    del config
    _ = obs
    return None
